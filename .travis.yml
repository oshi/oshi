language: java
script: mvn test
after_success:
  - if [  ]; then 
after_success:
  if [ "$TRAVIS_OS_NAME" == "linux" ]; then
    mvn clean test jacoco:report coveralls:report
  fi    
matrix:
  include:
    - os: linux
      jdk: oraclejdk8
    - os: osx
      osx_image: xcode8